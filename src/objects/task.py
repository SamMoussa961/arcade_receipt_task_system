
""" 
DESC: Task object
PARAM: $name: string, $points: int
    """
class Task:

    def __init__(self,_name: str, _points: int):
        self.name = _name
        self.score = _points

    def calculate(scores: list):
        score = 0
        
        if len(list) > 0:
            for i in list:
                score += i
        
        return score

